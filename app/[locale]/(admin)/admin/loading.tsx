import { RouteLoading } from "@/components/route-loading";

export default function AdminLoading() {
  return (
    <RouteLoading
      title="A carregar painel de administracao..."
      subtitle="Estamos a reunir as metricas da plataforma."
    />
  );
}
