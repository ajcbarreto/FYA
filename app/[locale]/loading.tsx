import { RouteLoading } from "@/components/route-loading";

export default function LocaleLoading() {
  return (
    <RouteLoading
      title="A carregar pagina..."
      subtitle="Estamos a preparar a melhor experiencia para ti."
    />
  );
}
